# ───────── Python ─────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/

# ───────── Entornos / secretos ─────────
.env
.env.local
.env.*.local
secrets/
*.json.key
client_secret*.json
token*.json
credentials*.json

# ───────── Datos y salidas locales ─────────
# Salidas, BD y temporales del pipeline. Se excluye TODO menos los datos
# sintéticos de prueba: CLAUDE.md los ubica en data/samples/ y la verificación
# obligatoria (`fill-template --data data/samples/credito_demo.json`) los
# necesita, así que ignorarlos dejaba esa comprobación sin poder correr fuera de
# la máquina de quien los creó. NUNCA meter datos reales de clientes ahí.
data/*
!data/samples/
output/
*.sqlite
*.sqlite-journal
*.db

# ───────── Build / packaging ─────────
dist/
build/
*.egg-info/
.eggs/
pip-wheel-metadata/

# ───────── IDE / Editores ─────────
.vscode/
.idea/
.DS_Store
*.swp
*.swo
*~
.~lock.*#

# ───────── Test / lint / coverage ─────────
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
.tox/

# ───────── Claude Code / workspace ─────────
.claude/
.claude-cache/

# ───────── Logs ─────────
*.log
logs/

# ───────── OS / temporales ─────────
Thumbs.db
ehthumbs.db
Desktop.ini
